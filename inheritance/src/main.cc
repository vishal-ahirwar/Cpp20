
// Auto Genrated C++ file by newton CLI
// Copyright 2023 Vishal Ahirwar //replace it with yout copyright notice!
#include "person.h"
#include "player.h"
#include <memory>
void pretty(const char*str)
{
    printf("\n\t\t\t...\n\t\t%s\n\t\t\t...\n",str);
};

int main(int argc, char *argv[])
{
    // std::shared_ptr<Person> person{new Person{"hemant", "wangdu", 34}};
    // std::shared_ptr<Player> player{new Player{"eleven", "oclock", 45, "football"}};
    // Person *person;
    // person = new Person(std::string("base"), std::string("class"), 23);
    // Player *player;
    // Person *anotherPerson = new Person(*person);

    // player = new Player(std::string("child"), std::string("class"), 405);
    // Player *anotherPlayer = new Player(*player);

    // pretty("Construction Completed!");

    // if (!person || !player)
    //     return -1;


    // person->printInfo();
    // player->printInfo();
    // player->Person::printInfo();

    // auto tempPtr = person;
    // person = player;

    // person->Person::printInfo();
    // person = tempPtr;

    // person->printInfo();

    // anotherPerson->printInfo();
    // anotherPlayer->Person::printInfo();

    // pretty("Destruction Started!");
    // delete player;
    // delete person;
    // delete anotherPerson;
    // delete anotherPlayer;
    // pretty("Destruction Completed!");

    pretty("|output:|");

    return 0;
};
